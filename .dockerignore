# Dependencias y artefactos (se regeneran dentro de la imagen)
node_modules
dist
.angular
coverage
documentation

# Control de versiones y metadatos
.git
.gitignore
.editorconfig

# Editor / SO
.vscode
.DS_Store

# Config local con la URL real: no debe hornearse en la imagen; el entrypoint
# genera config.json en runtime desde FIREBASE_DB_URL.
src/assets/config.json

# Docs y comprimidos que no forman parte del build
*.md
*.zip
*.rar
